# OpenAI Configuration
OPENAI_API_KEY=XXXX
USE_OPENAI=True

# Resume Filter Settings
MIN_KEYWORD_SCORE=50

# File Paths
# CSV_OUTPUT_PATH: Container path where CSV files are generated (don't change)
CSV_OUTPUT_PATH=/app/output/filtered_resumes.csv

# RESUME_FOLDER_PATH: Path for Excel hyperlinks in CSV output
# For Windows users: Set to your Windows host path where resumes are located
# Example Windows: D:\Work\GulfContractors\GC_resumefilter\resumes
# Example Linux: /home/user/GC_resumefilter/resumes
# Leave as container path (/app/resumes) if hyperlinks not needed
RESUME_FOLDER_PATH=/app/resumes

